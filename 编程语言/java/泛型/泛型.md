```java
List<Number> numList = new ArrayList();
List<Integer> integerList = new ArrayList();
numList = integerList; //在java中，这样是不被允许的

Integer integer = 10;
numList.add(integer); //这样是可以的

List<?> wildtypeList = new ArrayList();//这个List不能添加任何东西
```

