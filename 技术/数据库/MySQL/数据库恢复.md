```shell
# 备份当前错误状态（以防万一）
mysqldump lowcode_cook > /tools/old_cook.sql

mysql -e "DROP DATABASE lowcode_cook;"
mysql -e "RENAME DATABASE lowcode_cook_recovered TO lowcode_cook;"


mysqlbinlog \
  --skip-gtids \
  --database=lowcode_cook \
  --stop-position=65204 \
  /var/lib/mysql/mysql-binlog.000001 \
  /var/lib/mysql/mysql-binlog.000002 \
  /var/lib/mysql/mysql-binlog.000003 \
  /var/lib/mysql/mysql-binlog.000004 \
  /var/lib/mysql/mysql-binlog.000005 \
  > /tmp/lowcode_cook_RECOVERED.sql


  mysqldump -u root -p \
  --no-data \
  --skip-comments \
  --skip-add-drop-table \
  --single-transaction \
  lowcode_cook > /tools/lowcode_cook_schema_only.sql



  mysql -e " CREATE DATABASE lowcode_cook_recover;" -uroot -p


  mysql lowcode_cook < /tools/lowcode_cook_RECOVERED.sql
```

